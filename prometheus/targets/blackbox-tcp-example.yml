# Example blackbox TCP targets — duplicate as
# `blackbox-tcp-<your-site>.yml`. Useful for "is this port open?" probes
# (databases, SSH, custom TCP services).

- targets: []
  labels:
    kind: intranet

# - targets:
#     - 192.168.1.50:22
#     - 192.168.1.60:3306
#   labels:
#     kind: intranet
#     role: services
