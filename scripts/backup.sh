#!/usr/bin/env bash
# pi-tale — backup persistent data.
# Status: skeleton. Will tar and rotate ./data/* (or /mnt/datos/pi-tale/data)
# into ./backup/ with a date-stamped filename, preserving uids.
set -Eeuo pipefail

echo "backup.sh is not implemented yet."
exit 0
